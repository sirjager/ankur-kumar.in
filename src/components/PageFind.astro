---

---

<div class:list={["mx-auto h-full min-h-20 max-w-6xl"]}>
	<div id="searchui" class:list={["relative min-h-20 w-full"]}></div>
</div>

<script is:inline>
	window.addEventListener("DOMContentLoaded", (_) => {
		new PagefindUI({
			element: "#searchui",
			resetStyles: false,
			debounceTimeoutMs: 50,
			showImages: true,
			showEmptyFilters: true,
		});
	});
</script>
