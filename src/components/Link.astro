---
import type {HTMLAttributes} from "astro/types";

interface Props extends HTMLAttributes<"a"> {
	href: string;
	"aria-label": string;
	key?: string | number;
	nofocus?: boolean;
}

const {href, nofocus, "class:list": className, class: class_, ...attrs} = Astro.props as Props;
---

<a
	href={href}
	{...attrs}
	class:list={[
		class_ ?? className,
		{
			"rounded-btn outline-none ring-offset-white focus:outline-none focus:ring-2 focus:ring-accent focus:ring-offset-2 dark:focus:ring-offset-black":
				!nofocus,
		},
	]}
>
	<slot>Link</slot>
</a>
