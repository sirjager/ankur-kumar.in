---
import type {HTMLAttributes} from "astro/types";

interface Props extends HTMLAttributes<"a"> {
	href: string;
	"aria-label": string;
	key?: string | number;
}

const {href, "class:list": className, class: class_, ...attrs} = Astro.props as Props;
---

<a
	href={href}
	{...attrs}
	class:list={[
		class_ ?? className,
		"outline-none ring-offset-white focus:outline-none focus:ring-2 focus:ring-accent focus:ring-offset-2 dark:focus:ring-offset-black",
	]}
>
	<slot>Link</slot>
</a>
