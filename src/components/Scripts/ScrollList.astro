---

---

<script>
	if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
		const scrollers = document.querySelectorAll(".scroller");
		scrollers.forEach((scroller) => scroller.removeAttribute("data-animated"));
	}
</script>
