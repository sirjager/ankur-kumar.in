---
interface Props {
	className?: string;
}

const {className = ""} = Astro.props;
---

<button
	aria-label="change theme"
	class:list={[
		"group/tgthm relative overflow-clip px-5 py-3",
		"text-base-content hover:text-base-200",
		"transition-all duration-300 ease-in-out",
		"border border-neutral/20 dark:border-neutral-content/40",
		"after:transition-all after:duration-300 after:ease-in-out",
		"after:absolute after:h-full after:w-full after:bg-base-content after:content-['']",
		"after:-bottom-full after:left-0 hover:after:bottom-0",
		"rounded-btn after:rounded-btn",
		className,
	]}
>
	<span class="relative z-[1]">
		<slot>Button</slot>
	</span>
</button>
